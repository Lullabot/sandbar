{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "minimumReleaseAge": "3 days",
  "internalChecksFilter": "strict",
  "automerge": true,
  "customManagers": [
    {
      "description": "Track the molecule test container image; no built-in manager scans molecule.yml",
      "customType": "regex",
      "managerFilePatterns": ["/^molecule/.+/molecule\\.ya?ml$/"],
      "matchStrings": [
        "image:\\s*[\"']?(?<depName>[^:\"'@\\s]+):(?<currentValue>[^@\"'\\s]+)(?:@(?<currentDigest>sha256:[a-f0-9]+))?"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "Track the pinned GitLab CLI (glab) release in the dev-tools role",
      "managerFilePatterns": ["/^roles/dev-tools/defaults/main\\.yml$/"],
      "matchStrings": [
        "devtools_glab_version:\\s*\"(?<currentValue>[^\"]+)\""
      ],
      "depNameTemplate": "gitlab-org/cli",
      "datasourceTemplate": "gitlab-releases",
      "registryUrlTemplate": "https://gitlab.com",
      "extractVersionTemplate": "^v(?<version>.+)$"
    },
    {
      "customType": "regex",
      "description": "Track the pinned Drupal.org CLI (drupalorg) PHAR release in the dev-tools role",
      "managerFilePatterns": ["/^roles/dev-tools/defaults/main\\.yml$/"],
      "matchStrings": [
        "devtools_drupalorg_cli_version:\\s*\"(?<currentValue>[^\"]+)\""
      ],
      "depNameTemplate": "mglaman/drupalorg-cli",
      "datasourceTemplate": "github-releases"
    },
    {
      "customType": "regex",
      "description": "Track the Node.js major version used for the NodeSource apt repo in the base role",
      "managerFilePatterns": ["/^roles/base/defaults/main\\.yml$/"],
      "matchStrings": [
        "base_nodejs_major_version:\\s*(?<currentValue>\\d+)"
      ],
      "depNameTemplate": "node",
      "datasourceTemplate": "node-version",
      "versioningTemplate": "node"
    }
  ],
  "packageRules": [
    {
      "description": "Dependency bumps are chores, not user-facing fixes. config:recommended pulls in :semanticPrefixFixDepsChoreOthers, which types gomod's `require` deps as fix(deps); release-please then treats those as patch releases and lists them under Bug Fixes.",
      "matchManagers": ["gomod"],
      "semanticCommitType": "chore"
    },
    {
      "description": "Tagged action releases: cooldown, then automerge",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["minor", "patch"],
      "minimumReleaseAge": "3 days",
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "description": "Major action bumps: cooldown, then automerge",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["major"],
      "minimumReleaseAge": "3 days",
      "automerge": true,
      "automergeType": "pr"
    },
    {
      "description": "Pure digest moves: no timestamp available, always review",
      "matchManagers": ["github-actions"],
      "matchUpdateTypes": ["digest", "pinDigest", "pin"],
      "automerge": false,
      "dependencyDashboardApproval": true
    },
    {
      "description": "Node.js base image: only offer LTS (even-numbered) majors, and require manual review since these are major bumps",
      "matchDatasources": ["node-version"],
      "allowedVersions": "/^\\d*[02468]\\./",
      "automerge": false
    }
  ]
}
