---
# Optional initial project clone. Empty by default — set via
# `sand create --clone-url` or group_vars. GitHub authentication for the
# clone comes from the secrets role's file-backed git credential store (see
# roles/secrets), populated from a github-scoped secret in the host secrets
# store — not from a var here.
project_clone_url: ""
