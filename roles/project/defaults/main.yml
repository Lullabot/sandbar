---
# Optional initial project clone. Empty by default — set via
# `sand create --clone-url` or group_vars. project_clone_token is used for
# GitHub URLs (written to a per-org .env as GH_TOKEN and approved with
# direnv); other hosts clone without it.
project_clone_url: ""
project_clone_token: ""
