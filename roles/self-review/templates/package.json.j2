{
  "name": "sandbar-self-review",
  "version": "1.0.0",
  "private": true,
  "description": "Pinned install of upstream @self-review/serve, the browser review UI `sand land --review` starts inside a VM.",
  "dependencies": {
    "@self-review/serve": "{{ selfreview_version }}"
  },
  "overrides": {
    "@self-review/react": "npm:@self-review/types@{{ selfreview_version }}"
  }
}
