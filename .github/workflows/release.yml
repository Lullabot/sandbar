# Release workflow: on a `v*` tag push, GoReleaser cross-compiles `sand` for
# darwin/linux x amd64/arm64, uploads the archives to the GitHub Release, and
# updates the Homebrew formula in the `lullabot/homebrew-sandbar` tap.
#
# Tags are created by release-please (see release-please.yml), not by hand:
# merging its release PR creates the `vX.Y.Z` tag + GitHub Release, and — because
# release-please tags with a GitHub App token — that tag push triggers this
# workflow. GoReleaser runs in `release.mode: keep-existing`, so it uploads the
# binaries to release-please's existing release without touching its notes.
#
# Human prerequisites (one-time, performed by a release maintainer, NOT by
# this workflow):
#   1. Create the empty `lullabot/homebrew-sandbar` GitHub repository (this is
#      the Homebrew tap GoReleaser's `brews:` publisher pushes formula updates
#      to).
#   2. Provision a `HOMEBREW_TAP_GITHUB_TOKEN` Actions secret on this repo: a
#      token scoped to push access on `lullabot/homebrew-sandbar` only.
name: release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - uses: goreleaser/goreleaser-action@v6
        with:
          version: '~> v2'
          args: release --clean
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          HOMEBREW_TAP_GITHUB_TOKEN: ${{ secrets.HOMEBREW_TAP_GITHUB_TOKEN }}
