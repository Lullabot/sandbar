group_vars/all.yml
# Personal Lima config (image digests, paths) — generate VMs with `sand create`
/lima.yaml

# GoReleaser build output
dist/

# `go build ./cmd/sand` drops the binary at the repo root
/sand
